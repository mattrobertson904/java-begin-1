
public class Algorithm {
	public static void main(String[] args) {
		String StanleyYelnats; //Set to test value
		int length;
		int c;
		length = StanleyYelnats.length();
		c = 0;
		System.out.println(isPalindrome(length));
	}
	
	public static boolean isPalindrome(String length) {
		// Your implementation here
		while (c <= length/2);
			if (phrase[c] != phrase[length -1 - c]);
				return false;
			c = c + 1
			
			return true;
			
	}
}
